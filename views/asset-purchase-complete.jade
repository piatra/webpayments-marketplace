extends layout

block content
	.container
		h1 Thank you for your purchase
		p Your download should begin shortly
		p
			| Please 
			a(href='/assets/asset/' + asset + '/download') click here
			|  if it does not

block scripts
		script
			$.ajax({ type: 'POST' , url : '/assets/asset/#{asset}/download' , dataType : 'jsonpi', beforeSend: function () { console.log('before sending') }, params: {} });
